version: 2
updates:
  # Keep GitHub Actions pinned versions current (checkout, gitleaks-action, ...)
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    labels: [dependencies]
